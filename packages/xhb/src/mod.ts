/**
 * @module
 *
 * Parse and serialize HomeBank XHB files. Auto-generated from the HomeBank C
 * source using tree-sitter, with a schema-driven runtime engine.
 *
 * @example Parse an XHB file
 * ```ts ignore
 * import { parse } from "@hertzg/xhb";
 *
 * const xhb = parse(await Deno.readTextFile("example.xhb"));
 * ```
 *
 * @example Serialize an XHB object
 * ```ts ignore
 * import { serialize } from "@hertzg/xhb";
 *
 * const xml = serialize(xhb);
 * ```
 */

// TODO: Wire up public API once runtime and generated code are ready
export {};
